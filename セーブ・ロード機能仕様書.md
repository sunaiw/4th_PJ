# セーブ・ロード・終了機能 仕様書

## 0. 目的と決定事項

ゲームを中断・再開できるようにし、ゲーム内から正規の手順で終了できるようにする。
現状はタイトル画面の QUIT 以外に終了手段が無く（`KeyCode.Escape` の参照はコードベースにゼロ）、
再起動すると必ず Wave 1 から始まる。

| 項目 | 決定 |
|---|---|
| セーブスロット | **1枠のオートセーブのみ**（常に同一ファイルへ上書き） |
| セーブ契機 | **Setupフェーズの先頭で自動保存** + ポーズメニューの手動SAVE（Setup中のみ） |
| CO-OP対応 | **今回は対象外**。CO-OP中はセーブ機能を無効化する |
| 保存先 | `Application.persistentDataPath/savegame.json`（JsonUtility） |

### なぜ Setup フェーズ限定なのか

Defenseフェーズ中は敵の座標・HP・経路・各種デバフタイマー・飛翔中の弾・タワーの攻撃クールダウン・
Siege Markerのマーク対象など、多数の実行時状態が相互参照を持ったまま存在する。これを丸ごと直列化すると
実装量と不整合バグのリスクが跳ね上がる。Setupフェーズの先頭は敵も弾も存在しないため、
盤面の状態が「タワー群 + リソース + 獲得済み報酬」だけに還元でき、復元が決定的になる。

毎ウェーブ頭に自動保存されるため、実用上「毎回最初からになる」問題は完全に解消する。

---

## 1. 保存する状態

進行状態は4つのクラスに分散している。それぞれから吸い上げる。

### GameManager
- `currentWave`
- `life`
- `initialLife`（`IncreaseMaxLife()` で増えるため必須）
- `cost`
- `coreShieldActive`
- `killCounts[2]`

### RewardManager
- `acquiredRewardCounts`（RewardTypeごとの獲得数）

`Dictionary` は JsonUtility で直列化できないため、`List<RewardCountEntry>{ type, count }` に平坦化する。

### TowerManager
- 配置済みタワー全件（`activeTowers`）: `type` / `cellX` / `cellY` / `ownerId` / `currentHp` / `placedWave` / `requiresSupply` / `outpostNumber`
- `nextOutpostNumberByOwner[2]`
- `placedCountsInCurrentSetup`（手動SAVEでSetup途中に保存した場合に設置上限を正しく引き継ぐため）

### MapManager
**保存しない。** 壁の解放範囲は `ExpandMap(waveNumber)` の入力に対して決定的であり、
かつ解放範囲は Wave に対して単調増加する（シングルプレイの式 `minY = -(w/2+1)`, `maxY = (w+1)/2` は
いずれも単調）。したがってロード時に `ExpandMap(currentWave - 1)` を **1回だけ** 呼べば再現できる。
`currentWave <= 1` の場合は呼ばない。

---

## 2. 新規ファイル

### `Assets/Scripts/SaveData.cs`
JsonUtility 用の直列化データクラス群。すべて `[System.Serializable]`。

```csharp
[System.Serializable] public class TowerSaveData {
    public int type;            // (int)TowerType
    public int cellX, cellY;
    public int ownerId;
    public float currentHp;
    public int placedWave;
    public bool requiresSupply;
    public int outpostNumber;
}

[System.Serializable] public class RewardCountEntry { public int type; public int count; }
[System.Serializable] public class PlacedCountEntry { public int type; public int count; }

[System.Serializable] public class GameSaveData {
    public int saveVersion = 1;
    public string savedAtUtc;

    public int currentWave;
    public int life;
    public int initialLife;
    public int cost;
    public bool coreShieldActive;
    public int[] killCounts;

    public List<RewardCountEntry> rewardCounts;

    public List<TowerSaveData> towers;
    public int[] nextOutpostNumberByOwner;
    public List<PlacedCountEntry> placedCountsInCurrentSetup;
}
```

`saveVersion` が現行版と異なるセーブは「読めない」と判断して破棄する（互換維持のコストを払わない）。

### `Assets/Scripts/SaveSystem.cs`
静的クラス。ファイルIOと、シーン遷移をまたぐ受け渡しを担う。

```csharp
public static class SaveSystem {
    public const int CurrentVersion = 1;
    public static string SavePath { get; }            // persistentDataPath/savegame.json
    public static GameSaveData PendingLoad;           // タイトル→MainGame の受け渡し用。GameManagerが消費する

    public static bool HasSave();                     // ファイル存在 + パース可能 + バージョン一致
    public static GameSaveData Load();                // 失敗時 null（例外は握りつぶして Debug.LogWarning）
    public static bool Save(GameSaveData data);
    public static void DeleteSave();
    public static GameSaveData CaptureCurrentState(); // 各マネージャから吸い上げて組み立てる
}
```

- 読み書きは必ず `try/catch`。壊れたセーブでクラッシュさせない。
- 書き込みは一時ファイル `savegame.json.tmp` へ書いてから差し替え、書き込み中の異常終了で
  既存セーブを失わないようにする。

### `Assets/Scripts/PauseMenuUI.cs`
`GameManager.Start()` から `gameObject.AddComponent<PauseMenuUI>()` で生成する
（`GameSpeedController` などと同じ流儀）。UIは既存のUI群と同じく実行時に動的生成する
（`GameOverUI.CreateLayout()` のボタン生成コードを踏襲）。

- Canvas `sortingOrder = 250`（HUD既定 < Tutorial 100 < **PauseMenu 250** < GameOver 300）。
  GameOverより手前には出さない。
- `KeyCode.Escape` で開閉。ただし以下の間は開かない:
  - `GameManager.CurrentPhase == GamePhase.GameOver`
  - 自分がPause中でないのに `Time.timeScale == 0`（Tutorial等が制御中。
    `GameSpeedController.IsBlockedByOtherUI()` と同じ判定）
- 開いたとき `Time.timeScale` を退避して 0 に、閉じたとき退避値へ戻す。
  `GameSpeedController` は「自分がPauseしていないのに timeScale==0」を検知してボタンを自動で
  非活性にするため、そちら側の変更は不要。

ボタン構成:

| ボタン | 動作 | 非活性条件 |
|---|---|---|
| RESUME | メニューを閉じて timeScale を戻す | なし |
| SAVE | `SaveSystem.Save(SaveSystem.CaptureCurrentState())`。成功時「SAVED」を数秒表示 | Setupフェーズ以外 / CO-OP時 |
| SAVE & TITLE | 保存してからタイトルへ。保存できない状況ならラベルを `TITLE` にして保存せず遷移 | なし |
| QUIT GAME | `Application.Quit()`（`#if UNITY_EDITOR` では `EditorApplication.isPlaying = false`） | なし |

非活性のSAVEには理由を表示する:「Save is available during the Setup phase.」/
「Save is not available in CO-OP mode.」

タイトルへ戻る際は `GameOverUI.ResetPersistentGameManager()` と同じ後始末が必要
（`GameManager` は `PersistAcrossScenes => true` のため、明示的に破棄しないと遷移先で二重初期化になる）。
この処理は `GameOverUI` と重複するので、`GameManager` 側に `public static void DestroyPersistentInstance()`
を切り出して両者から呼ぶ形に整理する。

---

## 3. 既存ファイルの変更

### `GameManager.cs`

1. **ロード状態の適用**
   `Start()` の `InitializeGame()` を、`SaveSystem.PendingLoad != null` なら `ApplyLoadedState(data)` に分岐させる。
   `PendingLoad` は消費したら必ず `null` に戻す（リトライ時に誤って再適用しないため）。

2. **`GameLoopCoroutine` の Setup 冒頭をスキップ可能にする**
   ここが実装上の最大の注意点。Setup先頭のブロックは毎回 `cost` を再計算し `killCounts` を 0 に戻すため、
   ロード直後にそのまま走らせると保存した `cost` が破棄される。
   `private bool skipNextSetupResourceReset` を持たせ、ロード直後の初回 Setup のみリソース再配布を
   丸ごとスキップして保存値をそのまま使う。イベント（`OnCostChanged` 等）の発火はスキップしない。

3. **オートセーブ**
   Setup フェーズのリソース確定直後、`while (!setupPhaseFinished)` の待機に入る前に
   `if (!IsCoop) SaveSystem.Save(SaveSystem.CaptureCurrentState());` を呼ぶ。
   この時点では `killCounts` は既に 0 にリセット済みで、`cost` には撃破ボーナスが加算済みのため、
   復元は「保存された cost をそのまま使う」だけで正しくなる。

4. **`TriggerGameOver()` で `SaveSystem.DeleteSave()`**
   その周回は終了したので、CONTINUE で死んだ盤面に戻れないようにする。

5. `Start()` に `gameObject.AddComponent<PauseMenuUI>()` を追加。

6. 状態の入出力用に以下を追加:
   - `public void CaptureInto(GameSaveData data)`
   - `public void ApplyLoadedState(GameSaveData data)`
   - `public static void DestroyPersistentInstance()`

### `TowerManager.cs`

- `public void CaptureInto(GameSaveData data)` — `activeTowers` を走査して `TowerSaveData` を作る。
  セル座標は `MapManager.Instance.WorldToGrid(tower.transform.position)` で取得。
- `public void RestoreTowers(GameSaveData data)` — 復元専用の生成経路。
  既存の `SpawnTower()` は `ChangePlacedCount()` と `NotifyEnemiesToRecalculatePath()` を
  副作用として持つため流用しない。復元経路では:
  1. **Outpost(Barricade) を先に、その他を後に** 生成する
  2. `Instantiate` 直後（`Start()` より前）に `SetOwner()` / `SetOutpostNumber()` / `PrepareRestoredState()` を呼ぶ
  3. `MapManager.Instance.SetTowerOccupant(cellPos, true)` を各セルに対して呼ぶ
  4. 全件生成後に `nextOutpostNumberByOwner` と `placedCountsInCurrentSetup` を保存値で上書きする
     （生成過程で採番・カウントを動かさないこと）
  5. 最後に `RecalculateSupplyNetwork()` を1回呼ぶ

### `Tower.cs`

`currentHp` は初期化時に `maxHp` へ設定され、`Start()` の `UpdateStatsFromRewards()` で報酬倍率が
適用される。さらに `requiresSupply` と `placedWave` も `Start()` 内で「今の盤面・今のWave」から
自動決定される。復元時はこれらを保存値で上書きする必要がある。

`Instantiate` 直後（`Start()` 実行前）に呼ばれる復元用のフックを追加する:

```csharp
public void PrepareRestoredState(float hp, int wave, bool needsSupply)
```

内部に `pendingRestore` フラグと値を持たせ、`Start()` の**末尾**で以下を適用する:
- `placedWave = wave;`
- `requiresSupply = needsSupply;`（供給判定の自動決定より後で上書きすること）
- `currentHp = Mathf.Min(hp, maxHp);`（報酬による maxHp 変動を反映した後にクランプ）
- `healthDisplay.UpdateHPText(currentHp, maxHp)` で表示を同期

`requiresSupply` を保存する理由: 「盤面にOutpostが1つも無い間に置かれたタワーは恒久的に供給ルールの
対象外」という Step 2 の詰み対策を、復元時にも再現するため。単純に再生成すると、復元順序次第で
Outpost有りと判定され、本来Offline化しないはずのタワーが一斉にOfflineになる。

### `RewardManager.cs`

- `public void CaptureInto(GameSaveData data)` — `acquiredRewardCounts` を平坦化
- `public void ApplyLoadedState(List<RewardCountEntry> entries)` — 復元後に `OnRewardsUpdated` を発火

### `TitleScreenManager.cs`

ボタンを3つに増やし、位置を調整する:

| ボタン | anchoredPosition | 動作 |
|---|---|---|
| CONTINUE | (0, 40) | `SaveSystem.PendingLoad = SaveSystem.Load();` → `TutorialUI.SkipTutorial = true;` → MainGame をロード |
| START | (0, -80) | 新規開始。既存セーブがある場合のみ上書き確認オーバーレイを挟む |
| QUIT | (0, -200) | 既存のまま |

- `SaveSystem.HasSave()` が false のとき CONTINUE は `interactable = false` かつ暗色。
- CONTINUE のラベル下に小さく `Wave {n}` を出すと再開先が分かって親切（任意）。
- START 確定時に `SaveSystem.DeleteSave()` を呼ぶ。呼ばないと、新規開始直後に落ちた場合に
  古いセーブが残り、CONTINUE が前周回の盤面を指したままになる。

### `GameOverUI.cs`

- `ResetPersistentGameManager()` を `GameManager.DestroyPersistentInstance()` の呼び出しに置き換える。
- セーブ削除は `GameManager.TriggerGameOver()` 側で行うためUI側の追加処理は不要。

---

## 4. 復元の実行順序

Unity の `Start()` の実行順は `MapManager` と `GameManager` の間で保証されないため、
復元処理は **`GameLoopCoroutine` の先頭で `yield return null` を1回挟んでから** 行う。
これで全コンポーネントの `Start()` が完了していることが保証される。

1. タイトルで CONTINUE → `SaveSystem.PendingLoad` に読み込み、`MainGame` シーンをロード
2. 各コンポーネントの `Start()` が走る（`MapManager.InitializeGrid()` を含む）
3. `GameLoopCoroutine` 先頭で `yield return null`
4. **`RewardManager.ApplyLoadedState()`** … `Tower.Start()` の `UpdateStatsFromRewards()` が参照するため必ず先
5. `MapManager.ExpandMap(currentWave - 1)`（`currentWave >= 2` のときのみ）
6. `GameManager.ApplyLoadedState()`（wave / life / initialLife / cost / shield / killCounts）とイベント発火
7. `TowerManager.RestoreTowers()`
8. `skipNextSetupResourceReset = true` の状態で Setup フェーズへ入る

---

## 5. 動作確認項目

- [ ] Wave 1 で開始 → Wave 3 まで進めて終了 → CONTINUE で Wave 3 の Setup から再開できる
- [ ] 再開後、タワーの配置・所有者・HP・Outpost番号が保存前と一致する
- [ ] 再開後、獲得済み報酬のスタック効果（Damage UP 等）がタワーに反映されている
- [ ] 再開後、壁の解放範囲が保存前と一致し、敵がコアまで到達できる（経路が通っている）
- [ ] Outpost が無い状態で置いたタワーが、再開後も Offline にならない（`requiresSupply` の復元確認）
- [ ] ゲームオーバー後、タイトルの CONTINUE が非活性になる
- [ ] ESC でポーズメニューが開き、RESUME で元の再生速度（x1/x2等）に戻る
- [ ] Defense フェーズ中は SAVE が非活性で、理由が表示される
- [ ] CO-OP モードでは SAVE が非活性になる
- [ ] QUIT GAME でアプリが終了する（エディタでは再生停止）
- [ ] セーブファイルを手で壊しても CONTINUE が非活性になるだけでクラッシュしない

---

## 6. 今回対象外

- CO-OP モードのセーブ・ロード（2クライアント間の状態同期・照合の仕組みが別途必要なため）
- Defense / Reward フェーズ途中でのセーブ
- 複数スロット、セーブのサムネイル、プレイ時間記録
